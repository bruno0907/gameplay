import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: 49,
    height: 49,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 8,
    marginRight: 22,
    overflow: 'hidden',
  },
  avatar: {
    width: 45,
    height: 45,       
    borderRadius: 6, 
  }
});

export { styles };
